# 语音快记 - 产品发布网页

## 📁 目录结构

```
landing-page/
├── index.html          # 产品落地页主页面
├── css/
│   └── style.css       # 样式文件
├── images/             # 截图和素材（需手动添加）
│   └── .gitkeep
├── deploy.md           # 部署指南
└── README.md           # 本文件
```

## 🚀 部署方式

### 方案一：GitHub Pages（推荐）
1. 将代码推送到 GitHub
2. 进入仓库 Settings → Pages
3. Source 选择 `main` 分支，目录选择 `/landing-page`
4. 保存后等待几分钟即可访问

### 方案二：Vercel / Netlify
1. 连接 GitHub 仓库
2. 设置根目录为 `landing-page/`
3. 自动部署

### 方案三：本地预览
```bash
cd landing-page
python3 -m http.server 8080
# 浏览器访问 http://localhost:8080
```

## 📝 自定义内容

编辑 `index.html` 中的以下内容：
- App 名称和描述
- 功能特性列表
- 截图（放入 `images/` 目录）
- 下载链接（指向 GitHub Releases 的 APK）

## 🔗 APK 下载链接配置

在 GitHub 上创建 Release 并上传 APK 后，将下载链接更新到 `index.html` 中的下载按钮处。
