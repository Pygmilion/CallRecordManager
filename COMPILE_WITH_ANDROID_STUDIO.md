# 🎯 Android Studio 编译步骤（已安装）

## ✅ 你已经完成
- ✅ Android Studio 已安装
- ✅ 项目文件夹已打开

---

## 🚀 现在开始编译（5 个步骤）

### 步骤 1：打开 Android Studio

1. 打开 **Android Studio** 应用
2. 如果看到欢迎界面，点击 **"Open"**
3. 如果已经打开了其他项目，点击 **File → Open**

---

### 步骤 2：选择项目

1. 在文件选择器中，导航到：
   ```
   /Users/natsusakai/Documents/CallRecordManager
   ```
   
2. 选择 **CallRecordManager** 文件夹

3. 点击 **"Open"** 或 **"打开"**

4. 如果提示 "Trust Project"，点击 **"Trust Project"**

---

### 步骤 3：等待 Gradle 同步

**这一步很重要！必须等待完成！**

1. 项目打开后，Android Studio 会自动开始 **Gradle 同步**

2. 底部会显示进度：
   ```
   Gradle sync in progress...
   ```

3. **首次同步需要下载依赖**，可能需要 **5-15 分钟**

4. 等待直到看到：
   ```
   Gradle sync finished
   ```
   或底部状态栏显示 "Ready"

**⚠️ 如果同步失败**：
- 点击顶部的 🐘 图标（Sync Project with Gradle Files）
- 或者：File → Sync Project with Gradle Files

---

### 步骤 4：编译 APK

#### 方式 A：编译调试版（推荐）

1. 点击顶部菜单：**Build**

2. 选择：**Build Bundle(s) / APK(s)**

3. 点击：**Build APK(s)**

4. 等待编译（首次约 **3-5 分钟**）

5. 编译完成后会弹出通知：
   ```
   APK(s) generated successfully
   ```

6. 点击通知中的 **"locate"** 查看 APK 文件

#### 方式 B：直接运行到设备

如果你有安卓手机连接：

1. 用 USB 连接手机（USB 调试已开启）

2. 顶部设备选择器选择你的手机

3. 点击绿色的 **Run 按钮**（▶️）

4. 应用会自动安装并启动

---

### 步骤 5：找到 APK 文件

#### 在 Android Studio 中

1. 编译完成后点击通知中的 **"locate"**

2. 或者在左侧项目树中：
   - 切换到 **"Project"** 视图（左上角下拉菜单）
   - 展开：`app` → `build` → `outputs` → `apk` → `debug`
   - 右键 `app-debug.apk` → **Reveal in Finder**

#### 在 Finder 中

1. 打开 Finder

2. 按 `Command + Shift + G`

3. 输入路径：
   ```
   /Users/natsusakai/Documents/CallRecordManager/app/build/outputs/apk/debug
   ```

4. 按回车

5. 找到 **`app-debug.apk`** 文件

---

## 📱 安装 APK 到手机

### 方式 1：通过 Android Studio（最简单）

1. 连接手机（USB 调试已开启）
2. 点击 Run 按钮
3. 自动安装并启动

### 方式 2：通过 ADB

在终端执行：
```bash
adb install /Users/natsusakai/Documents/CallRecordManager/app/build/outputs/apk/debug/app-debug.apk
```

### 方式 3：文件传输

1. 将 `app-debug.apk` 传到手机
2. 在手机上点击安装

---

## 🔧 常见问题解决

### Q1: Gradle 同步失败

**错误信息**：Gradle sync failed

**解决方法**：

1. **检查网络连接**
   - 确保能访问互联网
   - 首次同步需要下载约 200-300 MB

2. **清理缓存**
   - File → Invalidate Caches / Restart
   - 选择 "Invalidate and Restart"

3. **重新同步**
   - 点击顶部的 🐘 图标

4. **检查 JDK**
   - File → Project Structure → SDK Location
   - 确保 JDK 版本 >= 17

---

### Q2: 找不到 SDK

**错误信息**：SDK location not found

**解决方法**：

1. 打开 `local.properties` 文件

2. 添加或修改：
   ```properties
   sdk.dir=/Users/你的用户名/Library/Android/sdk
   ```

3. 重新同步 Gradle

---

### Q3: 编译错误

**错误信息**：Build failed

**解决方法**：

1. **查看错误信息**
   - 底部 Build 窗口查看详细错误

2. **清理项目**
   - Build → Clean Project
   - 然后 Build → Rebuild Project

3. **检查 API Key**
   - 确认 `local.properties` 中有：
     ```properties
     STEPFUN_API_KEY=***REMOVED_API_KEY***
     ```

---

### Q4: 找不到 APK

**问题**：编译成功但找不到 APK

**解决方法**：

1. 在 Android Studio 中：
   - Build → Build Bundle(s) / APK(s) → Build APK(s)
   - 等待通知，点击 "locate"

2. 或者在终端执行：
   ```bash
   find /Users/natsusakai/Documents/CallRecordManager -name "*.apk"
   ```

---

### Q5: 应用闪退

**问题**：安装后打开立即闪退

**解决方法**：

1. **查看日志**
   ```bash
   adb logcat | grep "com.callrecord.manager"
   ```

2. **检查权限**
   - 在手机设置中授予应用权限

3. **重新编译**
   - Build → Clean Project
   - Build → Rebuild Project

---

## 📊 编译进度参考

### 首次编译时间

| 步骤 | 预计时间 |
|------|---------|
| 打开项目 | 30 秒 |
| Gradle 同步 | 5-15 分钟 |
| 编译 APK | 3-5 分钟 |
| **总计** | **8-20 分钟** |

### 后续编译时间

| 步骤 | 预计时间 |
|------|---------|
| 打开项目 | 10 秒 |
| Gradle 同步 | 10-30 秒 |
| 编译 APK | 1-2 分钟 |
| **总计** | **1-3 分钟** |

---

## 🎯 成功标志

### Gradle 同步成功

底部状态栏显示：
```
✓ Gradle sync finished in X s XXX ms
```

### 编译成功

通知显示：
```
✓ APK(s) generated successfully
  app-debug.apk
  [locate]
```

### APK 文件存在

文件位置：
```
/Users/natsusakai/Documents/CallRecordManager/app/build/outputs/apk/debug/app-debug.apk
```

文件大小：约 10-15 MB

---

## 💡 快速命令参考

```bash
# 查找 APK 文件
find /Users/natsusakai/Documents/CallRecordManager -name "*.apk"

# 安装 APK
adb install /Users/natsusakai/Documents/CallRecordManager/app/build/outputs/apk/debug/app-debug.apk

# 查看连接的设备
adb devices

# 查看应用日志
adb logcat | grep "com.callrecord.manager"

# 打开 APK 所在文件夹
open /Users/natsusakai/Documents/CallRecordManager/app/build/outputs/apk/debug
```

---

## 📱 APK 信息

### 文件信息
- **文件名**：`app-debug.apk`
- **位置**：`app/build/outputs/apk/debug/`
- **大小**：约 10-15 MB
- **类型**：调试版（未签名）

### 系统要求
- **最低版本**：Android 7.0 (API 24)
- **目标版本**：Android 14.0 (API 34)
- **架构**：通用（支持所有架构）

### 应用功能
- ✅ 录音文件扫描和管理
- ✅ 语音转文字（说话人分离）
- ✅ AI 智能纪要生成
- ✅ 结构化输出
- ✅ 本地数据库存储

---

## 🎉 完成后

恭喜！你已经成功编译了 Android 应用！

### 下一步

1. **安装到手机**
   - 通过 USB 或文件传输

2. **测试功能**
   - 扫描录音文件
   - 转写并生成纪要

3. **修改代码**（可选）
   - 修改 UI 或功能
   - 重新编译测试

---

## 📞 需要帮助？

### 查看日志

**Gradle 同步日志**：
- 底部 Build 窗口

**编译日志**：
- Build → Build Output

**运行日志**：
- 底部 Logcat 窗口

### 常见错误搜索

在 Android Studio 中：
- Help → Find Action
- 输入错误信息搜索

---

**现在开始：打开 Android Studio，选择项目文件夹！** 🚀
