# 📱 Android Studio 完整安装和编译指南

## 🎯 方案 1：使用 Android Studio（最推荐）

### 第一步：下载 Android Studio

#### 1. 访问官网
打开浏览器，访问：
```
https://developer.android.com/studio
```

#### 2. 下载对应版本
- 点击 "Download Android Studio" 按钮
- 选择 macOS 版本
- 文件名类似：`android-studio-2023.x.x.xx-mac.dmg`
- 大小：约 1.2 GB

#### 3. 等待下载完成
下载时间取决于网络速度，通常 10-30 分钟

---

### 第二步：安装 Android Studio

#### 1. 打开 DMG 文件
- 双击下载的 `.dmg` 文件
- 会弹出安装窗口

#### 2. 拖动安装
- 将 `Android Studio.app` 拖到 `Applications` 文件夹
- 等待复制完成（约 2-3 分钟）

#### 3. 首次启动
- 打开 `Applications` 文件夹
- 双击 `Android Studio`
- 如果提示"无法打开"，右键点击 → 打开 → 确认

#### 4. 导入设置
- 选择 "Do not import settings"
- 点击 OK

#### 5. 数据共享
- 选择是否发送使用统计（随意选择）
- 点击 Next

#### 6. 安装类型
- 选择 "Standard"（标准安装）
- 点击 Next

#### 7. 选择主题
- 选择 Light 或 Dark 主题
- 点击 Next

#### 8. 验证设置
- 确认安装内容
- 点击 Next

#### 9. 下载组件
- 点击 Finish
- 等待下载 Android SDK 等组件（约 10-20 分钟）
- 下载大小：约 2-3 GB

#### 10. 完成安装
- 下载完成后点击 Finish

---

### 第三步：打开项目

#### 1. 启动 Android Studio
- 如果已关闭，重新打开

#### 2. 打开项目
- 点击 "Open"
- 导航到：`/Users/natsusakai/Documents/CallRecordManager`
- 点击 "Open"

#### 3. 信任项目
- 如果提示 "Trust Project"，点击 "Trust Project"

#### 4. Gradle 同步
- 项目打开后会自动开始 Gradle 同步
- 底部会显示进度条
- 首次同步需要下载依赖，约 5-10 分钟
- 等待显示 "Gradle sync finished"

---

### 第四步：配置 API Key

API Key 已经配置好了，在 `local.properties` 文件中。

如果需要修改：
1. 在左侧项目树中找到 `local.properties`
2. 双击打开
3. 确认有：`STEPFUN_API_KEY=1FYEJ99v11hFMCS06Xoomi0GTxiEcwjB3fq1WjbLLwP2mdHO426dOt7IlAlvhGQtI`

---

### 第五步：编译 APK

#### 方式 A：编译调试版（推荐）

1. 点击顶部菜单：`Build`
2. 选择：`Build Bundle(s) / APK(s)`
3. 点击：`Build APK(s)`
4. 等待编译（首次约 3-5 分钟）
5. 编译完成后会弹出通知
6. 点击通知中的 `locate` 查看 APK

**APK 位置**：
```
app/build/outputs/apk/debug/app-debug.apk
```

#### 方式 B：直接运行到设备

1. 连接安卓手机（USB 调试已开启）
2. 顶部设备选择器选择你的手机
3. 点击绿色的 Run 按钮（▶️）
4. 应用会自动安装并启动

#### 方式 C：编译发布版

1. 点击：`Build` → `Generate Signed Bundle / APK`
2. 选择：`APK`
3. 点击：`Next`
4. 创建密钥库：
   - 点击 `Create new...`
   - Key store path: 选择保存位置
   - Password: 设置密码（记住！）
   - Alias: 输入 `mykey`
   - Password: 设置密钥密码
   - Validity: 25 年
   - 填写证书信息
   - 点击 `OK`
5. 选择 `release` 构建类型
6. 勾选 V1 和 V2 签名
7. 点击 `Finish`
8. 等待编译完成

---

### 第六步：找到 APK 文件

#### 在 Android Studio 中
1. 编译完成后点击通知中的 `locate`
2. 或者在左侧项目树中：
   - 切换到 `Project` 视图
   - 展开 `app` → `build` → `outputs` → `apk` → `debug`
   - 右键 `app-debug.apk` → `Reveal in Finder`

#### 在 Finder 中
1. 打开 Finder
2. 按 `Command + Shift + G`
3. 输入：`/Users/natsusakai/Documents/CallRecordManager/app/build/outputs/apk/debug`
4. 按回车
5. 找到 `app-debug.apk`

---

### 第七步：安装到手机

#### 方式 1：通过 Android Studio
1. 连接手机（USB 调试已开启）
2. 点击 Run 按钮
3. 自动安装并启动

#### 方式 2：通过 ADB
```bash
adb install /Users/natsusakai/Documents/CallRecordManager/app/build/outputs/apk/debug/app-debug.apk
```

#### 方式 3：文件传输
1. 将 APK 传到手机
2. 在手机上点击安装

---

## 🎯 成功标志

### Gradle 同步成功
底部显示：
```
Gradle sync finished in X s XXX ms
```

### 编译成功
通知显示：
```
APK(s) generated successfully
```

### APK 文件存在
文件位置：
```
app/build/outputs/apk/debug/app-debug.apk
```
大小：约 10-15 MB

---

## 🔧 常见问题

### Q1: Gradle 同步失败
**解决**：
- 点击 `File` → `Invalidate Caches / Restart`
- 选择 `Invalidate and Restart`

### Q2: SDK 未找到
**解决**：
- 点击 `Tools` → `SDK Manager`
- 确保安装了 Android 14.0 (API 34)
- 确保安装了 Build-Tools 34.0.0

### Q3: 编译错误
**解决**：
- 查看 `Build` 窗口的错误信息
- 点击 `Build` → `Clean Project`
- 然后 `Build` → `Rebuild Project`

### Q4: 找不到 APK
**解决**：
- 确认编译成功
- 切换到 Project 视图查找
- 或使用 Finder 搜索 `app-debug.apk`

---

## 📊 时间和资源

### 下载
- Android Studio: 1.2 GB (10-30 分钟)
- SDK 组件: 2-3 GB (10-20 分钟)
- Gradle 依赖: 200-300 MB (5-10 分钟)

### 安装
- Android Studio: 2-3 分钟
- SDK 组件: 10-20 分钟

### 编译
- 首次编译: 3-5 分钟
- 后续编译: 1-2 分钟

### 总计
- **首次完整流程**: 约 40-60 分钟
- **磁盘占用**: 约 5-6 GB

---

## ✅ 检查清单

- [ ] Android Studio 已下载
- [ ] Android Studio 已安装
- [ ] 项目已打开
- [ ] Gradle 同步成功
- [ ] API Key 已配置
- [ ] APK 编译成功
- [ ] APK 文件已找到
- [ ] 应用已安装到手机

---

**完成后你将拥有一个可以安装到安卓手机的 APK 文件！** 🎉
